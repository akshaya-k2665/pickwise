{
  "name": "fullstack-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "cd client && npm install && cd ../server && npm install",
    "build:client": "cd client && npm install && npm run build && cd ../server && npm install",
    "build": "npm run build:client",
    "start": "node server/server.js"
  },
  "devDependencies": {
    "vite": "^7.0.6"
  }
}
